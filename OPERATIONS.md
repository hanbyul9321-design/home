# O.HOME 운영 매뉴얼

이 홈(`https://home-plum-gamma.vercel.app`)을 **관리자로서 운영**할 때 보는 문서입니다.
설치 방법은 [README.md](./README.md), 설치 상세·문제해결은 [SETUP_GUIDE.md](./SETUP_GUIDE.md), 내부 동작 원리는 [SERVICE_LOGIC.md](./SERVICE_LOGIC.md)를 참고하세요.
이 문서는 **"평소에 뭘 어디서 관리하나"**만 다룹니다.

---

## 0. 지금 이 홈의 연결 정보 (요약)

| 항목 | 값 |
|---|---|
| 배포 주소 | `https://home-plum-gamma.vercel.app` |
| DB | Supabase — `https://mhuorklupfecfvtvwaan.supabase.co` |
| 연결 방식 | Vercel 환경변수 (`NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`) |
| 가입코드 | 기본값 `WELCOME` — 아래 3.4에서 바꿀 수 있음 |
| 관리자 | 가장 먼저 가입한 계정 (자동 지정) |

---

## 1. 로그인 · 관리자 화면 들어가기

1. `/login`에서 로그인
2. 로그인하면 상단/사이드에 **환경설정** 메뉴가 보입니다 (관리자만 보임)
3. **⚠️ 환경설정은 PC 전용입니다** — 모바일에서는 열람만 되고 편집은 안 됩니다

환경설정 화면은 왼쪽에 **탭 목록**이 있고, 오른쪽에 그 탭의 설정이 나옵니다. 대부분의 탭은 **바뀐 내용이 바로 미리보기에 반영**되지만, **[SAVE]를 눌러야 실제로 저장**됩니다 — 눌러야 방문자에게도 적용됩니다.

---

## 2. 환경설정 탭 — 무엇을 관리하나

| 탭 | 여기서 하는 일 |
|---|---|
| **디자인** | 테마 색상, 로고, 브라우저 탭 제목/아이콘, 역할별 폰트, 캐릭터 탭 리스트 색, 스위치 탭 색 |
| **메뉴 관리** | 메뉴 노출·순서·이름 변경, **메뉴별 공개범위**(전체/회원/관리자만), 메뉴별 부속 설정, 커스텀 링크, 미배치 기능 추가 |
| **메인 페이지** | 메인 화면에 뜨는 위젯 구성·순서 (PC/모바일 각각), 기본 구성으로 되돌리기 |
| **위젯** | 각 위젯의 세부 내용·설정값 |
| **게시판 관리** | 게시판 생성/삭제, 게시판별 말머리·스킨·권한, 갤러리 유형·말머리 뱃지 색 |
| **자관 질문** | 자캐 소개(자관) 문답에 쓸 질문 세트(CP/NCP) 관리 |
| **무드 리스트** | 다이어리 무드 아이콘·색 추가/수정/삭제/순서 |
| **마우스 커서** | 상태별 커서 이미지, 핫스팟, 전체 on/off |
| **TRPG** | 로그 열람 안내 문구, 도토리 상태 카테고리, 플레이기록 표시 열 |
| **메모장** | 스티커 메모 작성 권한, 작성자 표시 여부 |
| **회원/보안** | **가입코드 변경**, 회원 목록 확인·내보내기(삭제) |
| **데이터 백업** | 백업 다운로드, 복원, 쓰지 않는 이미지 정리, DB 이전, 초기화 |
| **포크 업데이트** | 원본 저장소의 새 버전을 받는 방법 안내 |
| **보안 규칙** | 지금 붙어 있는 DB의 스키마/보안 규칙 재확인·재복사 |

---

## 3. 자주 하는 운영 작업

### 3.1 공개범위(권한) 기준

| 대상 | 누가 볼 수 있나 |
|---|---|
| 전체공개 글 | 누구나 |
| 멤버공개 글 | 로그인한 회원만 |
| 나만보기 | 작성자 + 관리자만 |
| 수정 | 작성자 본인 또는 관리자 |
| 삭제 | 작성자 본인 또는 관리자 |
| 방명록 | 비로그인 방문자도 작성 가능 |
| 사이트 설정 | 읽기는 누구나, 변경은 관리자만 |
| 이미지 | 보기는 누구나, 올리기는 로그인 회원만 |

**메뉴를 "회원만 보임"으로 바꾸면 그 메뉴에 걸린 글도 최소 그 기준까지 자동으로 좁혀집니다** — 글 하나하나를 따로 바꿀 필요 없습니다. (메뉴 관리 → 권한 탭)

### 3.2 새 메뉴 열기 / 순서 바꾸기 / 숨기기

**환경설정 → 메뉴 관리 → 기본(구성) 탭**에서 노출 on/off, 드래그로 순서 변경, 이름 수정. **권한 탭**에서 그 메뉴의 공개범위를 정합니다.

### 3.3 게시판 추가

**환경설정 → 게시판 관리 → [게시판 추가]** — 이름·스킨·권한을 정하면 메뉴에 새 게시판이 생깁니다. 말머리도 여기서 관리합니다.

### 3.4 가입코드(초대코드) 바꾸기

**환경설정 → 회원/보안** → 가입코드 입력란에 새 값 입력 → 저장.
외부인이 무단으로 가입하는 걸 막는 유일한 장치라, **지인들과 공유한 코드가 새어나갔다 싶으면 바로 바꾸세요.** 바꾼 뒤에는 새 코드를 지인들에게 다시 알려줘야 합니다.

### 3.5 회원 관리

**환경설정 → 회원/보안 → 회원 목록**에서 태그를 달거나, 목록에서 내보낼(DELETE) 수 있습니다.

> ⚠️ **[DELETE]는 홈에 보이는 프로필만 지웁니다. 로그인 계정 자체는 지워지지 않습니다** — 그 사람은 계속 로그인할 수 있습니다. 계정 자체를 막으려면 Supabase 대시보드 → **Authentication → Users**에서 직접 삭제해야 합니다(관리자 키가 필요한 작업이라 홈 화면에서는 못 함 — 안 그러면 누구나 남의 계정을 지울 수 있게 됨).

### 3.6 백업 (정기적으로 할 것 — 권장: 월 1회 이상)

**환경설정 → 데이터 백업**

| 버튼 | 담기는 것 |
|---|---|
| ↓ 데이터만 | 글·캐릭터·자관·설정 + 이미지 전부 |
| ↓ 회원까지 | 위 + 회원 태그·가입코드 등 |

zip 파일 하나로 받아지므로 개인 클라우드/PC에 보관해 두세요.

### 3.7 쓰지 않는 이미지 정리

글을 지워도 이미지는 저장소에 남습니다(다른 글이 같은 이미지를 쓰고 있을 수 있어서). **데이터 백업 → 쓰지 않는 이미지 정리 → [찾아보기]**를 누르면 지금 아무도 참조하지 않는 파일만 골라 보여줍니다. Supabase 무료 저장 용량(1GB)이 차오르면 여기부터 확인하세요.

### 3.8 이 홈의 코드를 최신 버전으로 업데이트

**환경설정 → 포크 업데이트** 탭 안내를 따르거나, GitHub에서 직접:

1. 내 저장소(`hanbyul9321-design/home`) 페이지 위쪽 **[Sync fork] → [Update branch]**
2. Vercel이 자동으로 1~2분 안에 재배포

글·이미지·테마·회원 데이터는 전부 DB에 있으므로 **코드만 새것으로 바뀌고 데이터는 그대로**입니다.

### 3.9 데이터베이스 옮기기 / 초기화

- **DB 이전**: 데이터 백업 → 데이터베이스 이전 — 새 Supabase/Firebase 프로젝트로 통째로 복사. 옮기는 곳도 스키마를 미리 적용해 둬야 합니다. **회원 계정(비밀번호)은 옮겨지지 않습니다** — 새 곳에서 다시 가입해야 합니다.
- **초기화**: RESET → 지울 항목을 메뉴 단위로 골라서. **서버의 실제 데이터가 지워지니 신중하게.**

---

## 4. 정기 점검 체크리스트

| 주기 | 확인할 것 |
|---|---|
| 매달 | 데이터 백업(↓ 회원까지) 받아서 보관 |
| 가끔 | 쓰지 않는 이미지 정리 (저장 용량 확인) |
| 필요 시 | 가입코드가 새어나가지 않았는지, 회원 목록에 낯선 계정 없는지 |
| 7일 이상 접속 안 할 예정이면 | Supabase 무료 플랜은 **7일 무접속 시 프로젝트가 일시정지**됩니다 — 대시보드에서 한 번씩 접속하거나 재개 |
| 새 버전 나왔을 때 | [Sync fork]로 업데이트 (3.8) |

---

## 5. 자주 겪는 문제

| 증상 | 확인 |
|---|---|
| 방문자에게 빈 홈으로 보임 | `ohome.config.json`을 안 올렸거나, Vercel 환경변수 등록 후 **Redeploy**를 안 한 경우 |
| 방문자에게 기본 테마로 보임 | 데이터 백업 탭에 "서버에 올리지 않은 설정 N건" 알림이 있으면 눌러서 올리기 |
| 글은 보이는데 이미지가 안 보임 | Storage 보안 규칙이 게시됐는지 (환경설정 → 보안 규칙에서 재확인) |
| 로그인 창에서 이메일/비밀번호 오류 | Firebase는 Authentication에서 이메일/비밀번호 로그인을 켜야 함 (Supabase는 기본 지원) |
| 저장 용량이 계속 참 | 지운 글의 이미지가 남아 있을 수 있음 → 3.7 쓰지 않는 이미지 정리 |
| 홈이 전반적으로 느림 | Vercel Function Region이 Seoul(icn1)인지 확인·재배포 |
| Supabase가 며칠 뒤 갑자기 안 열림 | 무료 플랜 7일 무접속 일시정지 — 대시보드에서 재개 |
| "가입코드가 올바르지 않습니다" | 가입코드 오타이거나, 관리자가 최근에 바꾼 값 (3.4에서 확인) |
| 환경설정이 모바일에서 안 열림 | 의도된 동작 — PC에서만 편집 가능 |

더 자세한 설치 단계별 문제는 [SETUP_GUIDE.md 7장](./SETUP_GUIDE.md#7-문제가-생기면)을 참고하세요.

---

## 6. 절대 하면 안 되는 것

- `public/ohome.config.json`을 만들었다면 **절대 GitHub에 공개로 올리지 마세요** — 내 DB 연결 정보라 유출되면 남이 내 DB에 붙을 수 있습니다. (지금은 env 방식이라 이 파일 자체가 없음)
- Supabase의 `anon`/`publishable` 키가 아니라 **`service_role`/`secret` 키를 절대 Vercel이나 코드에 넣지 마세요** — 이건 관리자 권한 키라 노출되면 DB 전체가 위험해집니다.
